# Project Setup
PROJECT_NAME="Smart Study Assistant"

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=smart_study_db

# Construct the URL based on the above (for SQLAlchemy)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/smart_study_db

# Security
SECRET_KEY=generate_a_random_string_using_openssl_rand_hex_32
